//C++ program to add two number entered by user

#include<iostream> //header file
using namespace std; //namespace
int main(){ //main function
    int x,y; //declaring two variable
    cout<<"Enter first no.:"; //display this on screen
    cin>>x; //store the value entered by user
    cout<<"Enter second no.:";
    cin>>y;
    cout<<"Addittion of your no. "<<x<<" and "<<y<<" is "<<x+y; //printing the addition
    return 0;
}
