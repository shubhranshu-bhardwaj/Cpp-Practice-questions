// Write a program to find the length of string.
#include <iostream>
#include <string> //if we add string in our program then we have to add this library
using namespace std;
int main()
{
    string txt = "Adarsh";
    cout << "Length of string is:" << txt.length(); // for calculating the length of string we use string name.length()
    return 0;
}